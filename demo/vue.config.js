module.export = {
    /* css: {
        loaderOptions: {
            css: {
               rules: [{
                   test: /\.css$/i,
                   use: ['style-loader', 'css-loader','sass-loader'],
               }, ],
            }
        }
    } */
    /* devServer: {
        after:require('./mock/test.js')
    } */
}